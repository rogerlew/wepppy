"""Lightweight shim for CAO modules used by tests.

This package provides a minimal subset of the `cli_agent_orchestrator`
API so tests in `services/cao/test/*` can import without installing the
separate CAO package or adjusting `PYTHONPATH`.
"""

