# QA Review - Climate Multiple-Build Finalize Lock

## Metadata

- **Package**: `docs/work-packages/20260820_climate_finalize_lock/`
- **Reviewer**: Codex validation pass
- **Date**: 2026-08-21
- **Branch**: `codex/rehydrate-lfs-runtime`

## Results

| Gate | Result | Evidence |
| --- | --- | --- |
| Focused climate regressions | Pass | `wctl run-pytest tests/nodb/test_climate_multiple_build.py tests/nodb/test_climate_gridmet_multiple_build_service.py tests/nodb/test_climate_build_helpers.py tests/nodb/test_climate_facade_collaborators.py --maxfail=1` — `57 passed`, 8 warnings |
| NoDb persistence baseline | Pass | `wctl run-pytest tests/nodb/test_base_boundary_characterization.py tests/nodb/test_base_unit.py tests/nodb/test_base_misc.py --maxfail=1` — `83 passed`, 8 warnings |
| Climate stub/API surface | Pass | `wctl run-stubtest wepppy.nodb.core.climate`; `wctl check-test-stubs` — no issues / all stubs complete |
| Documentation | Pass | Contract and work-package `wctl doc-lint` runs — 0 errors, 0 warnings |
| Python syntax and whitespace | Pass | Changed Python files compile; `git diff --check` passes |
| CWD/fixture ordering regression | Pass | Omni orchestration followed by the full peakflow census module — `25 passed, 1 skipped`; broader Omni + peakflow slice — `119 passed, 1 skipped` |
| Full repository suite | Environment-qualified pass | The Docker canary contract test now explicitly skips when the runner's Docker CLI lacks the Compose v2 plugin, so this expected environment limitation no longer fails the suite. The previously reported Topanga failure was an order-sensitive CWD leak, not a missing manifest; Omni and peakflow now pass together in the failing order (`25 passed, 1 skipped`). The prior repository sweep excluding the Docker smoke test and before this follow-up completed with `6087 passed, 61 skipped`; a post-fix full sweep was not rerun. |

## Regression Coverage

- Real temporary Climate NoDb same-size unrelated rewrite is preserved.
- Real temporary Climate NoDb relevant station rewrite supersedes finalization.
- Malformed observed-year rewrite supersedes without publishing derived output.
- Daymet collection failure leaves derived controller fields unchanged.
- GridMET and Daymet collaborators return explicit derived results and share the finalizer.
- Omni scenario orchestration restores process CWD on success and failure.
- Peakflow census repository artifacts resolve from `__file__`-anchored paths.
- Docker canary contract validation explicitly skips when Compose v2 is
  unavailable; the companion workflow contract test remains runnable without
  the local Compose capability.

## Verdict

- **Gate status**: pass; Topanga CWD/fixture follow-up is resolved. The Docker Compose smoke test now skips explicitly when Compose v2 is unavailable.
- **Unresolved QA findings**: none in focused implementation scope.
- **Auxiliary isolation checker**: not completed; its default mode launched five full-suite randomized runs, so it was stopped after the targeted ordering regression passed.
- **Canary/deployment**: not executed; explicitly outside this request's authority.

**Compose capability test**: `wctl run-pytest
tests/docker/test_canary_smoke_contract.py --maxfail=1` — `1 passed, 1
skipped` because Compose v2 is unavailable in the runner.
