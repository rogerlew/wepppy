# QA review: RQ cancellation directory locks

Reviewer: /root/cancel_impl_qa, 2026-09-10 UTC.
Scope: production helper maintainability, meaningful cancellation tests and
test-resource isolation. Checkpoint ancestor: 3903cb778.

## Finding and disposition

QA-01, medium: warm-stop timeout on an early probe failure skipped cleanup of
infinite jobs, detached writers and Redis artifacts; acquisition began before
the protected block.

Resolved: acquisitions are inside the protected block. Probe subreaper
containment plus independent cold-stop/reap teardown runs after bounded warm
shutdown even before writer pidfds were captured. An early_failure scenario
proves no residual child, directory lock, or queue. Reviewer confirmed closure.

Non-blocking review comments were also addressed: old crashed writer survives
through replacement cancellation; configured Redis automatically enables real
tests in normal wctl gates; cold-killed probe worker registrations are removed
only for the unique test queue; all expected owned locks and execution identities
are asserted before cancellation so thread acquisition failures cannot pass.
README wording now explains automatic enablement.

## Verdict

QA passed with no remaining production-code quality findings. Validation gates passed: full suite 8202 passed/83 skipped and final focused
suite 17 passed; see 20260910_validation.md.
