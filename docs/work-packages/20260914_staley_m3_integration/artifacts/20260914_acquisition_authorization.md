# Bounded source acquisition authorization

On 2026-09-14 the owner explicitly authorized bounded source acquisition under
`source_delivery_proposal.md`. This supersedes earlier records marking that
operational permission unapproved; it does not change scientific policy.

Authorized scope: the exact original USGS THICK object and read-only NRCS SDA
collection/version query, derived from the selected project's DEM extent and
original in-basin MUKEYs. Initial live target is `addicted-reservist`; the entry
point must remain reusable for other eligible basins. Retain requests, responses,
native window, hashes and failures under module-owned visible preparation inputs.

All recorded limits remain mandatory: 96 MiB aggregate THICK response bodies,
30 seconds per request, 120 seconds overall, identity-pinned ranges, no full
object download, redirects or automatic retries; SDA request and response each
at most 1 MiB with a 30-second deadline. Prove enforcement before live execution.

No soil rebuilding, shared cache refresh/write, replacement horizons, new
service/dependency, expanded acquisition limits or production deployment is
authorized. Network permission is resolved; verification and implementation
requirements are not waived. Acquisition is not yet claimed complete.
