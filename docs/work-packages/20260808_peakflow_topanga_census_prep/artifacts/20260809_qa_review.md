# QA Review - Durable Peak-Flow Census Preparation

## Acceptance Evidence

- The focused reusable-engine suite passes 9 tests using the local repository
  virtual environment and a non-full root-volume temporary directory.
- The existing Phase 2A pilot suite passes 5 tests.
- Immutable evidence validation reports 64 complete terminals, 64 exact
  one-input mutations, 14,157 outer-joined rows, 30 baseline-only rows, 25
  mutant-only rows, 697 candidate rows, and 61 candidate trials.
- The synthetic site discovers noncontiguous hillslopes 7, 42, and 905 under
  non-Topanga scenario names. Its plan is deterministic and does not require a
  code edit.
- The accepted eight-hillslope pilot selection plans exactly 64 eligible
  records.
- An explicit one-trial validation run completed with the pinned observer. Its
  trace and hillslope-pass hashes exactly match the Phase 2A burned h106
  Ksat-minus evidence.
- The canonical Topanga plan was regenerated byte-identically and independently
  validates. It contains 1,120 requested records, 1,088 eligible records, and
  32 paired-cover exclusions.
- Schema validation, Python compilation, diff whitespace checks, and broad
  exception checks pass.

## Environment Exception

Canonical `wctl run-pytest` currently cannot create the session secret fixture
because the container root filesystem has no available `/tmp` capacity. The
exact error is `OSError: could not create numbered dir with prefix pytest- in
/tmp/pytest-of-unknown after 10 tries`. This is the known environment condition
named by the ExecPlan, not a test failure in the changed code. Focused tests
were rerun with `TMPDIR=/home/workdir/tmp-test` through `.venv/bin/pytest`.

## Verdict

**Pass with an environment note.** Generated and immutable evidence satisfy the
focused scientific and data-contract gates. The repository-wide container gate
must be reported as blocked, not passing.
