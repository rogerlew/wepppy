# QA Review - Batch and Culvert Climate Rehydration

## Scope and disposition

- **Package**: `docs/work-packages/20260904_batch_culvert_climate_rehydration_b/`
- **Reviewer**: Codex (independent QA pass)
- **Date**: 2026-09-05
- **Source**: Forest development host, `master` at `87559fe26`, with the
  implementation in the working tree
- **Disposition**: Changed-scope gates pass. The repository-wide run is
  conditionally accepted because it stopped on one unrelated pre-existing
  shape-converter compose contract failure. Forest has a successful smaller
  batch receipt; culvert full-workflow receipts remain limited by existing
  fixture defects.

## Test evidence

Focused and adjacent regression commands passed:

- `wctl run-pytest tests/rq/test_climate_rehydration.py --maxfail=1` - 10
  passed.
- `wctl run-pytest tests/rq/test_climate_rehydration.py tests/rq/test_batch_rq_retry_selection.py --maxfail=1` - 36 passed.
- `wctl run-pytest tests/rq/test_culvert_rq_nodir_guards.py --maxfail=1` - 5
  passed.
- `wctl run-pytest tests/culverts/test_culvert_orchestration.py tests/rq/test_culvert_rq_pipeline.py --maxfail=1` - 7 passed.
- `wctl run-pytest tests/nodb/test_batch_runner.py --maxfail=1` - 4 passed.

The direct Climate boundary tests use real temporary `.nodb` files, real
filesystem signatures, the real `clear_nodb_file_cache` implementation, real
Climate hydration, and real `NoDbStaleWriteError` detection. The same-size
interleaving case passes for both batch and culvert helpers.

The repository gate was run as `wctl run-pytest tests --maxfail=1` and stopped
after **5,078 passed and 50 skipped** at:

`tests/shape_converter/unit/test_runtime_hardening.py::test_prod_wepp1_overlay_does_not_override_shape_converter_hardening`

That test rejects the committed `shape-converter` service in
`docker/docker-compose.prod.wepp1.yml`. Neither file was changed by this
package. All changed batch, culvert, and Climate rehydration tests had already
passed in the same run.

## Static and documentation gates

- `python3 tools/check_broad_exceptions.py --enforce-changed --base-ref origin/master` - pass; no net broad-exception delta in the two changed production Python files.
- `python3 tools/code_quality_observability.py --base-ref origin/master` - pass as observe-only; generated reports were restored because they are unrelated outputs.
- `wctl doc-lint --path docs/work-packages/20260904_batch_culvert_climate_rehydration_b --path PROJECT_TRACKER.md` - pass after the review and acceptance artifacts were added.
- `git diff --check` - pass.
- `wctl check-rq-graph` - not applicable. The diff changes no enqueue site,
  function target, queue, dependency edge, or worker routing.
- npm gates - not applicable; no frontend files changed.
- stub gates - not applicable; no public stub/API surface changed.

## QA findings

| ID | Severity | Finding | Disposition |
| --- | --- | --- | --- |
| QA-01 | Low | Full suite has the unrelated shape-converter compose mismatch described above. | Accepted as a baseline deviation for this package; no changed-scope failure found. |
| QA-02 | Low | Forest culvert fixtures do not support a full downstream-success receipt; the successful batch receipt is recorded. | Resolved as an external fixture limitation; no target stale-write recurrence observed in three culvert attempts. |

No High or Medium QA findings remain. QA-02 is resolved as a release
condition, not a code defect.

## Sign-off

- **QA verdict**: `pass with conditions`
- **Reviewer**: Codex, 2026-09-05
