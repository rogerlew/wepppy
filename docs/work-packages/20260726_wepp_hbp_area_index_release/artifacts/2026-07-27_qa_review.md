# Independent QA Review

## Final Disposition

Approved for deployment after WEPPpy vendoring is pushed. Production
regeneration must create matching-version HBP shards before watershed
aggregation.

## Findings and Dispositions

- **High, closed:** The first incident replay reused older HBP shards. The
  replacement gate regenerated all 587 shards with `wepp_260727_hill` and
  consumed them with `wepp_260727`.
- **Medium, closed:** Release metadata advertised HBP schema 1.0 while the
  current writer emits 2.0. Commit `ca47d1fc` corrects the generator, Forest
  sidecars, documentation, and focused regression coverage; WEPPpy sidecars
  carry the same correction.
- **Medium, accepted maintenance debt:** The PS-04 fixture inspection utility
  remains schema-1-only. It is not in the deployed execution path. The native
  reader, WEPPpyo3 conversion, and exact-release replay provide production
  schema-2 evidence.
- **Operational condition, incorporated:** Forest must regenerate base and
  any configured Omni scenario HBP shards with the deployed hillslope binary
  before the matching watershed binary runs. The ExecPlan now names the exact
  hashes and run-scoped command with four-worker concurrency.

## Independent Evidence

- Complete Forest suite: 211 passed with two warnings.
- Permanent hillslope watchlist: 21 of 21 passed.
- Replacement 43-year host smoke passed for both binaries.
- Same-build replay contained 4,109 positive Hill rows, minimum `0.020 ha`,
  hillslope 587 `0.080 ha`, empty stderr, and a success marker.
- WEPPpyo3 LOSS and SOIL conversion rejected zero records.
- Vendored binaries and sidecars are byte-identical to the Forest release.
