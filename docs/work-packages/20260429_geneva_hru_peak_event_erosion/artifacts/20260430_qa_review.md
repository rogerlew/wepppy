# QA Review - Geneva HRU Peak Runoff and Choropleth Measure

Date: 2026-04-30 (UTC)
Reviewer: Codex

## Required QA Focus

1. One-HRU parity (`hru_peak_runoff` equals watershed peak).
2. Multi-HRU non-area-split behavior.
3. Legacy artifact behavior.
4. UI measure selection and API request wiring.

## Findings

No medium/high QA findings.

## Evidence

### 1) One-HRU parity

- Rust test: `cn::tests::one_hru_peak_runoff_matches_watershed_peak_discharge`
- Result: PASS
- Assertion: `hru_excess[0].peak_runoff_m3_s == summary_metrics.peak_discharge` within tolerance.

### 2) Multi-HRU non-area-split behavior

- Rust test: `cn::tests::multi_hru_local_peaks_are_not_area_split_watershed_peak`
- Result: PASS
- Assertion: local HRU peaks diverge from naive watershed area-split peak.

### 3) Legacy artifact behavior

- Python tests:
  - `test_query_rows_returns_legacy_unavailable_when_artifact_missing`
  - `test_query_rows_accepts_hru_peak_runoff_measure_id`
- Result: PASS
- Behavior: legacy missing artifact returns unavailable envelope with empty rows.

### 4) UI measure selection and API wiring

- Jest test: `geneva_summary_report.test.js` map test case.
- Result: PASS
- Assertions:
  - map measure selector includes `hru_peak_runoff`
  - `POST .../query/geneva/hru_map_rows` sends `measure_id=hru_peak_runoff` on selection
  - legend updates with `HRU peak runoff` label.

## Gate Decision

- QA gate: PASS
- Outstanding QA action items: none
