# Codex Phase 3 Review — Findings and Dispositions

**Date**: 2026-07-21 11:30 UTC
**Reviewer**: Codex (MCP, read-only sandbox), dispatched at Roger's direction; thread `019f829d-1045-7c62-b28c-8ce24fd73768`
**Scope**: Phase 3 QMD/builder, report_service, controller render stage, Docker, tests, and the rendered artifact
**Reviewer verdict**: request changes (11 findings: 3 high, 5 medium, 3 low)
**Disposition evidence class**: Executional — post-fix suite **105 passed** (now including a real Quarto render integration test); live RQ job `92ee701a` re-rendered on the dev stack; headless Playwright verified dynamic labels, exact-threshold initial state, and zero console errors; broad-exception gate PASS.

| # | Sev | Finding (condensed) | Disposition |
|---|-----|---------------------|-------------|
| 1 | High | Report re-solve dropped configured `slope_range`/`severity_filter` — filtered runs would report different selections/costs than their artifacts. | **Accepted, fixed.** Payload filters now feed the QMD's solve (with the same None→±inf bound substitution the controller uses). The integration test runs a filtered config and asserts the report's exact-threshold row reproduces the job payload's cost and selections. |
| 2 | High | Treatment subsets unsupported: the 3D cell raised unless ≥3 per-cell treatment groups, and the map JS hardcoded the three default tier labels. | **Accepted, fixed.** The 3D cell's per-treatment geometry extraction was dead code (the surface only consumes scalars) — removed, which also fixes subsets. The map JS now takes `treatmentLabels` from config for legend and tooltips (seam-marked); labels verified dynamic in the headless check. Residual: >3 treatments still map to 3 tier color slots (JS layer structure); noted for the UI phase. |
| 3 | Med | Grouped `area` alias copied hectares but the tooltip labels it "Area (ac)" (167.85 shown where ~414.77 is correct). | **Accepted, fixed — with a correction to the suggested fix.** Converting the alias alone would have made the report's re-solve double-convert (the seam wrapper auto-detects `area` first and multiplies by 2.47105). The alias is now acre-converted for display AND the solve runs on a frame with the alias dropped, so the wrapper converts exactly once. Verified: post-fix render cost unchanged ($513,272.61). |
| 4 | High | The "user-selected" sweep point silently snapped to the nearest integer grid cell — configured 48.2 displayed the 48 cell (~$2.05M) instead of the actual $513k run. | **Accepted, fixed.** The exact configured-threshold result (from the report's seam solve) is appended as a sweep row, so the map's initial state is the actual run (verified: `initialSddc: 48.2`, exact row present); the 3D marker plots the exact thresholds + actual cost instead of snapping. Residual: JS nearest-cell fallback for pairs missing due to excluded error cells remains (upstream slider behavior); noted. |
| 5 | Med | Absent channels layer crashed the links block (`Path(None).name`) despite being declared optional. | **Accepted, fixed.** Channels link renders "Unavailable" when absent; empty-string guard keeps the folium/interactive fallbacks on their existing missing-file paths. |
| 6 | Med | Run-derived JSON embedded in `<script>` without `</script>`/U+2028/U+2029 escaping — injection surface once served inline. | **Accepted, fixed.** `_script_json()` escapes `</`, U+2028, U+2029 for both `PATH_REPORT_CONFIG` embeds; the 3D surface spec JSON gets the same `</` escaping. |
| 7 | Med | Staged geojsons followed symlinks with no containment or content checks into the future public report tree. | **Accepted, fixed.** `_copy_run_geojson()` rejects symlinks, paths resolving outside the run directory, non-JSON, and non-GeoJSON content. Three new tests (symlink, traversal, content) assert Quarto never launches on validation failure. |
| 8 | Low | Staging dir leaked when staging itself failed; timeout killed only the Quarto parent, not the Jupyter kernel. | **Accepted, fixed.** `mkdtemp` moved into the protected lifecycle (leak regression test added — it failed against the first fix attempt and drove the correct structure); Quarto now runs in its own process group with `killpg` on timeout. |
| 9 | Low | Builder didn't enforce upstream provenance: no hash pin, first-occurrence anchors, unbounded replaces. | **Accepted, fixed.** Upstream SHA-256 pinned (fails loudly with re-review instructions); every anchor's occurrence count pinned; all patches count-1 with uniqueness asserts. The new check immediately surfaced a real ambiguity (`_parse_json_like` defined twice upstream), now handled explicitly. |
| 10 | Low | Quarto `.deb` version-pinned but not integrity-pinned. | **Accepted, fixed.** `QUARTO_SHA256` (from the official release checksums) verified via `sha256sum -c` before install in both dev and prod Dockerfiles; dev image rebuilt with the verifying layer. |
| 11 | Med | No automated test executed Quarto or the QMD — kernel discovery, execution, assets, and serialization were outside regression coverage. | **Accepted, fixed.** `test_report_render_integration.py` (markers: integration + slow; module-skips when the Quarto CLI is absent) performs a real render from committed fixtures (austere parquet + real WGS geojsons, ~185 KB added) and asserts the report/downloads/assets plus finding-1's filtered-consistency contract. A fast companion asserts every sweep JSON column parses per the browser contract. Runs in the container suite (~25 s of the 44 s total). |

## Notes

- The disposition run reproduced the full chain live: suite 105 green → worker restart → RQ job `92ee701a` finished (`path/report/PATH_CE_Report.html`, cost unchanged) → headless Playwright green (dynamic legend labels, `hasExactRow: true`, `initialSddc: 48.2`, canvas + sliders + 3D surface, zero console errors).
- Finding 3 is the second time this package's ha↔ac seam produced a subtle interaction (Phase 1's finding 1 established it); the QMD now documents the double-conversion hazard at the solve seam.
- No findings rejected; residuals (3-color tier cap; JS nearest-cell fallback for missing pairs) recorded above and carried to the Phase 4 UI/security work.
