# QA Review - Frozen Topanga Census Execution

## Evidence Reconciliation

The frozen selection regenerates byte-identically with 1,088 unique eligible
IDs. Current evidence has 1,088 complete, zero failed, and zero stopped terminals.
The terminal ledger covers every selection ID. The outer ledger contains
225,654 rows with 320 baseline-only and 298 mutant-only rows; absent values are
null. The candidate ledger is the exact 11,506-row candidate subset covering
1,027 trials. Version-two denominators include all 1,120 requested records and
32 exclusions. The storage manifest inventories 24,265 retained non-lock
artifacts with locator, size, SHA-256, format, and retention policy.

## Recovery and Repeatability

Complete reuse revalidates scientific bindings and retained hashes. Retry tests
preserve prior terminal/run attempts and reject mismatched bindings; unknown
statuses fail rather than retry. Plan and trial locks reject concurrent ownership.
Two full aggregation passes returned summary ID
`e5348e2e26adafc2fb858c0b96d8b5f7bb4dc1821e0f878f376c904a9d47f547`
and identical ledger hashes; no immutable file was replaced.

## Test Results and Limitation

- Host focused suite: 18 passed, 2 warnings.
- Canonical-container focused suite after final hardening: 17 passed, 1 skipped;
  the skip is the host-absolute Topanga mount unavailable in that container.
- Phase 2A canonical-container suite: 5 passed.
- Documentation lint: 5 files validated, zero errors or warnings before final
  lifecycle files were added; it is rerun at closeout.
- Canonical broad suite collected 6,074 tests but failed at setup because
  `/tmp/pytest-of-unknown` could not be created. An alternate basetemp advanced
  to 50 passes and 13 skips, then a direct `TemporaryDirectory()` again failed
  at full `/tmp`. The canonical suite is not reported as passing.

## Verdict

PASS. Independent revalidation found no remaining QA blocker; all 24,265
manifest records and all denominator/candidate totals reconcile.
