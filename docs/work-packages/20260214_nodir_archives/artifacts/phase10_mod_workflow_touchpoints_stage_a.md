# Phase 10 Mod Workflow Touchpoints (Stage A)

## Scope Guardrails
- Phase 10A only (docs-only).
- NoDir allowlist stays unchanged: `landuse`, `soils`, `climate`, `watershed`.
- Canonical NoDir error semantics remain fixed: `409 NODIR_MIXED_STATE`, `500 NODIR_INVALID_ARCHIVE`, `503 NODIR_LOCKED`.

## Touchpoint Inventory

| surface | file:function | roots touched | class (read/mutate/fs-boundary/serialized-path hazard) | current behavior | readiness (archive-ready/projection-helper required/owner-wrap required/blocked) | owner wave |
| --- | --- | --- | --- | --- | --- | --- |
| Treatments enqueue route | `wepppy/microservices/rq_engine/treatments_routes.py:build_treatments` | `landuse`, `soils` (transitive via enqueue target) | mutate, fs-boundary | Parses payload, updates `landuse.mapping`, optionally writes uploaded treatment raster, enqueues `build_treatments_rq`; no NoDir preflight before enqueue. | blocked | W1 |
| Treatments RQ owner job | `wepppy/rq/project_rq.py:build_treatments_rq` | `landuse`, `soils` | mutate | Uses `mutate_roots(wd, ("landuse", "soils"), ...)` around `Treatments.build_treatments()`. | archive-ready | W1 |
| Treatments mutation implementation | `wepppy/nodb/mods/treatments/treatments.py:build_treatments` | `landuse`, `soils`, `watershed` (read for translator/subwta) | mutate, fs-boundary | Writes `landuse/*.man` and `soils/*.sol`, updates dom maps/parquet, assumes writable dir-form roots inside owner-managed mutation context. | owner-wrap required | W1 |
| Ash enqueue route | `wepppy/microservices/rq_engine/ash_routes.py:run_ash` | `climate`, `watershed`, `landuse` (transitive via `Ash.run_ash`) | read, mutate, fs-boundary | Parses ash inputs/uploads, stores ash settings/maps, enqueues `run_ash_rq`; no NoDir preflight for dependent allowlisted roots. | blocked | W1 |
| Ash RQ job | `wepppy/rq/project_rq.py:run_ash_rq` | `climate`, `watershed`, `landuse` | read, fs-boundary | Calls `Ash.run_ash(...)` and WEPP interchange without explicit NoDir preflight/projection boundary at job start. | projection-helper required | W2 |
| Ash model execution | `wepppy/nodb/mods/ash_transport/ash.py:Ash.run_ash` | `climate`, `watershed`, `landuse` | read, fs-boundary | Directly opens `climate.cli_path` and reads watershed/landuse-driven inputs; path-heavy climate read is dir-form biased. | projection-helper required | W2 |
| Debris enqueue route | `wepppy/microservices/rq_engine/debris_flow_routes.py:run_debris_flow` | `watershed`, `soils` (transitive via enqueue target) | read | Validates payload and enqueues `run_debris_flow_rq`; no NoDir preflight for dependent allowlisted roots. | blocked | W1 |
| Debris RQ job | `wepppy/rq/project_rq.py:run_debris_flow_rq` | `watershed`, `soils` | read | Calls `DebrisFlow.run_debris_flow(...)` directly; relies on route/job caller for boundary checks. | archive-ready | W1 |
| Debris computation | `wepppy/nodb/mods/debris_flow/debris_flow.py:DebrisFlow.run_debris_flow` | `watershed`, `soils` | read | Consumes NoDb attributes (`area_gt30`, `ruggedness`, `clay_pct`, `liquid_limit`) without direct allowlisted-root file-path reads. | archive-ready | W1 |
| Omni scenarios enqueue route | `wepppy/microservices/rq_engine/omni_routes.py:_run_omni` | `climate`, `watershed`, `landuse`, `soils` (transitive via Omni execution) | mutate, fs-boundary | Parses scenarios/uploads, mutates omni state, enqueues `run_omni_scenarios_rq`; no canonical NoDir preflight on dependent roots. | blocked | W1 |
| Omni contrasts enqueue route | `wepppy/microservices/rq_engine/omni_routes.py:_run_omni_contrasts` | `climate`, `watershed`, `landuse`, `soils` (transitive via contrast execution) | read, mutate, fs-boundary | Builds contrast definitions/uploads and enqueues `run_omni_contrasts_rq`; no canonical NoDir preflight on dependent roots. | blocked | W1 |
| Omni contrast dry-run route | `wepppy/microservices/rq_engine/omni_routes.py:_dry_run_omni_contrasts` | `watershed` (direct), `landuse`, `soils`, `climate` (transitive) | read, fs-boundary | Builds dry-run report with watershed backend checks and omni parsing; no NoDir preflight for allowlisted roots. | blocked | W1 |
| Omni scenarios RQ coordinator | `wepppy/rq/omni_rq.py:run_omni_scenarios_rq` | `climate`, `watershed`, `landuse`, `soils` | mutate, serialized-path hazard | Orchestrates scenario jobs and dependency state; delegates root-form handling to clone/scenario internals. | blocked | W3 |
| Omni contrasts RQ coordinator | `wepppy/rq/omni_rq.py:run_omni_contrasts_rq` | `climate`, `watershed`, `landuse`, `soils` | read, serialized-path hazard | Orchestrates contrast jobs and landuse-skip logic; delegates root-form handling to `_run_contrast` path. | blocked | W3 |
| Omni scenario clone | `wepppy/nodb/mods/omni/omni.py:_omni_clone` | `climate`, `watershed`, `soils`, `landuse` | read, mutate, fs-boundary, serialized-path hazard | Handles `climate/watershed` as dir-or-archive symlink, but copies `soils` only in dir form and uses directory-skeleton assumptions for other roots. | blocked | W3 |
| Omni sibling clone replacement | `wepppy/nodb/mods/omni/omni.py:_omni_clone_sibling` | `landuse`, `soils` | read, mutate, fs-boundary, serialized-path hazard | Hard-removes and copytrees sibling `landuse/` and `soils/` directories, with no `.nodir` source path handling. | blocked | W3 |
| Omni contrast runner | `wepppy/nodb/mods/omni/omni.py:_run_contrast` | `climate`, `watershed`, `landuse`, `soils` | read, mutate, fs-boundary, serialized-path hazard | Symlinks `climate/watershed` dir-or-archive and merges `landuse/soils` parquets via `pick_existing_parquet_path`, but still depends on upstream clone/root-form assumptions. | blocked | W3 |
| Observed blueprint routes | `wepppy/weppcloud/routes/nodb_api/observed_bp.py:submit_task_run_model_fit` | none (allowlisted roots not directly referenced) | read, mutate, fs-boundary | Reads uploaded observed CSV text, runs observed fit/report endpoints against observed/wepp output assets. | archive-ready | W4 |
| Observed processing | `wepppy/nodb/mods/observed/observed.py:calc_model_fit` | none (allowlisted roots not directly referenced) | read, fs-boundary | Uses `observed/`, WEPP output interchange files, and optional SWAT outputs; no direct allowlisted-root path coupling identified. | archive-ready | W4 |
