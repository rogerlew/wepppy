# S01 actual direct and queued native acceptance

**Direct and live queued native PASS.** Independent correctness
execution after the coordinated development restart of source`de3a1eba0`.
Production/test code was not edited. No named project was used as a model work
directory. This is the bounded Omni generated-artifact/runtime gate, not closure
of every inventory item or the complete work package.

## Direct workflow and outputs

The full independent Grizzly copy is
`/wc1/batch/qa-omni-native-20260917-a61f3e/runs/qa-omni-native-20260917-a61f3e-grizzly`.
The original generic leaf correction, canonical destination-only fork marker
reset, source identities and source/config/binary hashes are retained in the
preparation records. Actual execution used UID1000/GID993 with the configured
mounts and umask. The caller set the existing WEPP CPU limit to4; individual
other native components retain their own normal concurrency.

The synthetic low/high SBS controls preserve the real source footprint, CRS,
nodata mask and canonical palette. They intentionally change class values and
do not represent the historical fire's class distribution. Both use the same
selected upload pathname; the second restores the first modification time.
Normal `parse_scenarios` and direct `run_omni_scenarios` execute Disturbed,
landuse/soil acquisition and generation, management/soil/run preparation,
hillslope and watershed WEPP, interchange, exports and reports.

| Check | Actual evidence |
| --- | --- |
| Full scientific extent |217 hillslopes,46 years1980–2025;218 generated `p*` files per extension includes watershed`pw0` |
| Low source receipt |`fe70fafea3c718c45a1a7f91b2c3c311cd936b8923f84c3d73ed7ded6656d380` |
| High source receipt |`742f5eb7190e76f3270f143a045bd845f9e40497d161d56dece52b0da05e69f9` |
| Native executions |Both executed and consumed their upload;393.20s and386.46s observed durations |
| Generated propagation |203 management and217 soil files changed; watershed output changed |
| Numerical output control |Six of11 watershed values changed; annual hillslope soil loss697.8→8643.3tonne/yr and outlet sediment discharge1651.2→9863.4tonne/yr |
| Scientific parameter control |Retained`p1` diff changes actual soil conductivity20→15, erodibility0.00008→0.0001 and management numerical lines, beyond header dates |
| Complete model output |217 hill rows,11 watershed output rows,230 all-year class rows; all46 years retained |
| Consumed-source reuse |Both subsequent invocations report skipped; generated numerical file hashes and physical versions remain identical |
| Retained artifacts |2,812 and2,806 regular child files, with shared links recorded inside the disposable copy |
| Named source after direct |All3,916 files/6,375,730,842 bytes rehashed unchanged against original versions and hashes |

Evidence: `runtime_omni_sbs_native_initial.log`,
`runtime_omni_sbs_native_result.json`,
`runtime_omni_sbs_generated_parameter_diff.txt`,
`runtime_omni_sbs_output_comparison.json`, and the complete disposable-root
`omni-native-manifest.json`/`generation-1`/`generation-2` records. The summary JSON
binds the full manifest by SHA-256. Durations observed while other runtime checks
overlap are not independent performance budget measurements.

The normal second clone reset logged an EBUSY deferred cleanup warning and left
its renamed stale directory. It then built and validated the new generation
successfully. That warning and retained directory remain; no manual cleanup or
production workaround was applied.

## Queued workflow and retained initial preflight

The initial queued attempt failed before any upload restoration or queue
admission because a scheduled access-log compiler changed physical metadata for
four byte-identical operational source files after the direct check. The exact
failed manifest/log, original baseline, four old/current identities/hashes,
scheduler/job trace and independent security disposition are retained in
`runtime_omni_ttl_attribution.md` and
`runtime_source_operational_drift_security_disposition.md`.

The explicit revision2 harness checks original SHA, length and mode for only
`TTL`, `disturbed.log`, `ron.log`, `watershed.log`; every other file retains its
strict original version. Regular-file membership and complete verification
interval guards remain checked, and final verification hashes every source file.
Physical exceptions are recorded rather than called whole-tree immutability.
No baseline or source file is repaired, no scheduler task is changed, and the
failed first attempt remains intact.

Revision2 uses canonical tracked developer admission on the existing `batch`
queue because the batch HTTP route intentionally does not enqueue. It does not
claim HTTP submission parity. Actual root
`c32f7273-81d6-450d-b688-5d08bdf2e4f8` finished its complete four-job tree:
dispatcher, native scenario worker, compilation and finalization. The queued
receipt and accepted child association match supplied low-class bytes. Five
actual Parquets match direct generation1 exactly in values and dtypes:
11 watershed-output rows,217 hillslope rows,87 channel rows,230 class/year rows
and9,982 hillslope/year rows. The finalizer stamped completion.

Consumed-source root`27efd5e3-64e5-4de9-bc64-5f02d41e01bc` finished its complete
three-job tree without a scenario worker. The scenario reported skipped and
all generated numerical hashes/physical versions remained unchanged. Actual
worker identities, times, registered children, dependencies, results, canonical
job-info and polling histories are retained as
`runtime_omni_sbs_rq_{changed_upload,consumed_skip}_{tree,jobinfo,poll}` artifacts.
`runtime_omni_sbs_rq_result.json` binds the full final manifest by SHA-256 and
records both phases, source proof and unchanged production module hashes.

Final verification rehashed all3,916 original source files/6,375,730,842 bytes;
every original byte is unchanged. Only the same four reviewed operational
physical identities differ, with original mode and length preserved. Every
other source version and whole regular-file membership remains unchanged.
The complete verification interval was stable. The actual RQ generation's
2,809 regular artifacts are retained under
`rq-acceptance-revision2/generation`.

This closes the assigned S01 direct/native/dispatcher/worker acceptance scope.
It does not assert HTTP batch enqueue support, quiet performance measurements,
arbitrary raster dependency closure, or closure of remaining inventory risks.
