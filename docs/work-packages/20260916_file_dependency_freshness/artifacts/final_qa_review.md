# Final independent QA review: accepted scope and explicit limitations

Reviewer: `freshness_qa`, 2026-09-17. **Final scoped QA PASS:** implementation
quality, ratified component budgets and the assigned completed runtime workflows
are accepted with the explicit failures and justified-unresolved inventory below.
No assigned runtime gate remains pending. This is not a claim that every
inventoried defect is fixed. No runtime or test changes were made for this
consolidation; overall package disposition and ExecPlan/tracker remain root-owned.

## Completed evidence

| Boundary | Retained acceptance |
| --- | --- |
| Independent implementation quality and performance | `quality_performance_consolidation.md` and its source-bound component reviews retain the accepted limits, original failed budgets, explicit CLI/Geneva amendments, bounded512-entry digest reuse, zero settled payload reads where measured, and remaining complexity/retained-storage debt. No new limits are inferred from concurrent canaries. |
| Broad repository checks | Actual `final_full_suite_quiet.log`:8,924 passed,99 skipped in1,192.22s. `final_npm_test.log`:899 tests/112 suites passed; frontend lint passed. Earlier wall-clock failure remains retained. Stub/broad-exception/RQ graph gates are separately recorded by root. |
| Restart parity | `runtime_restart_verification.json` records rebuilt/recreated service image, ordinary UID/GID/groups and preserved mount parity at implementationde3a1eba0. No local native result substitutes for this deployment-boundary evidence. |
| M3/WEPP hard-link behavior | `runtime_m3_hardlink_acceptance.json`:18 actual jobs finished, including native WEPP/interchange/water-balance/exports;218 CLI files changed ctime/link count,0 changed hashes, same accepted M3 attempt remains current. The2019–2021 scientific window yields an explicitly partial M3 result; unavailable longer return intervals are not presented as complete science. |
| Actual climate change and M3 rerun | `runtime_postfire_climate_changed.json` plus `runtime_browser_climate_changed.json` show stale state and visible rerun guidance. Normal API/RQ rerun produces a new accepted ID and current state in `runtime_postfire_rerun_current.json`/`runtime_browser_rerun_current.json`; both browser/reload checks have zero page errors. |
| Browser/controller bundle | `runtime_browser_after_wepp.json`: actual run page/reload current, matching served/rendered build ID, zero page errors. This verifies the C10/runtime integration, not active GL cache generations. |
| C02 shipped mixed native profiles | `runtime_features_dtale_qa_acceptance.md`:20 actual executions,16 immutable native artifacts and4 cache hits across shipped CSV/GPKG profiles. Real GeoJSON/Parquet/NoDb/Unitizer changes agree with fresh outputs; all3,916 named source files/6.376GB rehashed unchanged. No substituted catalog or numerical reader. |
| C11 public D-Tale workflow | Same review:65 checks/28 public HTTP calls; actual browse redirect, old-grid conflict, current schema/rows after same-ID relaunch, registered GeoJSON refresh/removal. Four real browser phases pass with zero page errors and visible guidance screenshots; no response interception/internal-loader shortcut. |
| C08/C09 and M3 presentation/downloads | `runtime_reports_after_wepp_complete.json`: landuse HTML/CSV200, water-balance HTML200, five M3 artifacts200, and provenance-bearing water-balance cache download200 with SHA matching disk. Water-balance CSV500 is retained as failed. |
| Bounded Geneva/archive observability | `runtime_archive_acceptance_3a8be19a8e1e.json` passes native canonical roundtrip/mode-byte parity/restored geometry reuse; actual public browse and failed-artifact downloads pass with matching hashes. The complete API/RQ sequence has separate evidence below. |
| Actual archive/restore API/RQ and recovery | Both jobs finished;545 selected artifacts and84 directory modes remain equal, and actual ZIP selected modes match. Restored M3 stale is independently attributed solely to unchanged soil bytes/new physical inventory metadata (PF-R01). Normal API/RQ rerun then publishes new attempt7902bf9bc67840b180df10ee22329450/current; browser/reload/report query and all five M3 downloads pass. See `runtime_archive_restore_soil_qa_review.md`. Byte/mode preservation plus explicit recovery passes; automatic restored currentness remains a failed/disclosed limitation. |
| SBS profile event fidelity | `runtime_profile_http_acceptance.json` records real capture/promotion/preauthenticated replay with two distinct multipart payloads matching server hashes. Canonical CLI401/exit0 remains a separately retained auth/reporting limitation. |
| Actual `/wc1` filesystem helper | `runtime_nfs_digest.json`: hard-link add/remove, touch/readable chmod and1,000 equal-size/restored-time rewrites produce0 digest mismatches. This is one host/service's NFS observation, not a cross-host coherence guarantee. |
| S01 direct and live queued native Omni | `runtime_omni_sbs_correctness_acceptance.md`:217 hillslopes/46 years, same-name low/high uploads propagate to203 management and217 soil files plus numerical outputs. Both consumed-source direct invocations skip. Actual four-job RQ execution matches five direct-generation Parquets exactly; three-job consumed reuse has no scenario worker and preserves numerical hashes/versions. Canonical tracked developer admission is used; HTTP batch enqueue is not claimed. |
| Interleaved full-state observation | `runtime_interleaved_state.json`:30 calls per independent copy,12,840 added production-digest cache hits,0 new misses/uncached payloads,395/512 entries. Means739.48/862.19ms; first calls2.225/2.648s. `runtime_interleaved_state_qa.md` accepts the measured warm-read/coexistence clause and scopes latency, initial current/stale states, instrumentation and capacity correctly. |

The public D-Tale browser's first two failed attempts used the earlier prototype
message text in the selector. Revision2 retained the correctly visible current
message and screenshot; revision3 matches the returned guidance and completes
all phases. Features' initial attempt stopped before copying on the tightened
shared fixture leaf-identity guard; revision2 corrects only that fixture name.
These original failures remain evidence and are not recast as product defects.

Omni's initial queued preflight also remains failed: a scheduled operational
writer changed physical metadata for four byte-identical named files. The
independent security disposition permits audit-only exceptions for exactly
`TTL`, `disturbed.log`, `ron.log` and `watershed.log`, requiring original SHA,
length and mode. The final queued audit hashes all3,916 original files unchanged;
every other file retains its strict original version and membership is unchanged.
Whole-tree physical immutability is explicitly false, not silently inferred
from byte preservation. See `runtime_source_operational_drift_security_disposition.md`.
No source or original baseline was repaired. Normal EBUSY deferred cleanup leaves
the documented stale child directory retained for observation.

## Finite inventory disposition and residual risk

`implementation_disposition.md` and `operation_matrix.md` now bind the actual
runtime evidence to each covered boundary. The inventory remains finite to the
retained Python/non-Python searches and maintained workflow traces. Confirmed
scientific false-current/false-stale behavior is not relabeled safe:

- C01 indirect VRT/directory/archive-child changes remain a confirmed native
  closure limitation. The complete small GTiff RAP/held-lock pass does not
  resolve it (`derived_indirect_closure_correctness_disposition.md`).
- C02's missing representative mixed-profile experiment is now complete.
  Arbitrary native dependency closure outside those actual profiles remains
  unproven; input formats are not silently restricted to make the audit pass.
- C07's fixed-receipt numerical stale mechanism is confirmed, but an ordinary
  maintained producer missing completion invalidation is not demonstrated.
  S01 same-name SBS reuse is a separate boundary.
- Roads, AgFields and Geneva whole preparation retain specific verified
  ingestion/completion/rebuild paths plus explicit unresolved wider workflows
  in `remaining_inventory_disposition_qa.md`. Generic native capability is not
  evidence of a supported-path defect.
- B-F01 active GL can mix generations. Related browser cache families remain
  separately unconfirmed. The smallest future decision is the documented open
  analysis refresh/snapshot UX contract; current new-tab/reload recovery does
  not make an already-open view coherent.
- PF-R01 soil logical identity still has confirmed physical-only false stale,
  now including actual archive restoration of soil/prepared inputs and SQLite
  main/WAL/SHM. Independent read-only hashes match all seven manifest-bound
  sources, all three archived cache files, and accepted main/WAL snapshot
  copies. A deepcopy-only comparison isolates the raw soil inventory as the
  sole freshness failure; it does not mutate or authorize acceptance. The
  [post-fire README](../../../../wepppy/nodb/mods/postfire_debris_flow/README.md#developer-and-operator-notes)
  documents restore→stale and explicit Run M3 recovery. No polling-time writer,
  persistent cache or implicit logical equivalence policy was introduced.

The water-balance CSV route has an **existing unimplemented adapter defect**,
independently attributed to baseline7d78e9810 by unchanged route/base-class
hashes and absent iterator overrides. It is documented durably in
[`wepppy/wepp/reports/README.md`, Hillslope Water Balance → Current CSV limitation](../../../../wepppy/wepp/reports/README.md#current-csv-limitation).
Recovery is the HTML report followed by project browse/download of its compact
Parquet summary; the download itself does not revalidate source currentness or
apply display-unit conversion. `runtime_watbal_csv_qa_disposition.md` preserves
the exact failure attribution and smallest separate adapter follow-up.

## Final acceptance and follow-ups

Changed-climate/stale/rerun, live archive/preservation/explicit rerun recovery,
direct/live queued Omni and interleaved state now have actual retained results.
No newly agreed whole-state deadline exists:739.48/862.19ms are observations of
local `get_state(..., reconcile=False)` calls, not HTTP latency or deadline passes.
The historical695ms run and individual component limits cannot establish such
a deadline. The60 calls show coexistence below capacity; actual512-entry eviction
acceptance comes from separate component probes. Zero production-digest payload
reads does not mean zero filesystem, NoDb, JSON or Parquet-footer reads.

Scoped QA finds no additional changed-code quality blocker in the accepted
boundaries above. Remaining debt is concrete:
large orchestration/provenance functions, observe-only complexity coverage
without installed radon, intentionally retained attempt storage, profile stress
capture's narrow mean budget margin, existing CSV/CLI issues, and the unresolved
inventory findings. None warrants an unmeasured redesign during closeout.
Promote retained deterministic authority/retention probes when next extending
their boundary, and make the unresolved read-set/UX decisions before expanding
freshness claims. Root may consume this final QA PASS with these limitations;
the original failures and ratified budget amendments remain unchanged.
