# Upload Handling Standard (Moved)

This document moved to:

- `docs/schemas/upload-endpoint-contract.md`

Use the schema document as the canonical source for upload endpoint limits, extension allowlists, validators, and scope notes (including `shape_converter` exclusion and culvert ZIP handling requirements).
