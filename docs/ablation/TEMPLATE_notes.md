# Ablation Notes: <incident_id>

## Context

- `incident_id`: `<YYYYMMDD_runid_scope_signature>`
- `runid`:
- `config`:
- `error_file`:
- `host_role`: `dev|prod`
- `host_name`:
- `container_service`:
- `binary_path`:
- `shared_context_files_staged`: `yes|no`
- `shared_context_inventory`: `<comma-separated file list from runs dir>`
- `fuzzy_case_set_source`: `<incident seeds/watchlist/release sentinels>`

## Operator Log (Chronological)

Use one entry per meaningful action:

```text
[YYYY-MM-DD HH:MM:SS TZ] ACTION
Command:
<exact command>
Observation:
<result summary>
Artifacts:
<relative paths>
```

## Assumptions and Corrections

- assumption:
- correction:
- evidence:

## Lane Notes

### Lane A: <name>

- goal:
- cases executed:
- conclusion:

### Lane B: <name>

- goal:
- cases executed:
- conclusion:

## End-of-Session Summary

- best current hypothesis:
- unresolved blockers:
- next first command:
- fuzzy regression status:
- fuzzy failure ledger path:
