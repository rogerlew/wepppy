# Current Open Items

This file tracks the remaining work before the current staging package is ready to be transferred into the official ITI template.

## Transfer Blockers

1. Freeze evaluator identity / responsible team for the production-bound issue.
2. Freeze the production-bound repository snapshot and deployment environment.
3. Convert `docs/ui-docs/acr-draft-int.md` into the official `VPAT 2.5Rev INT` template structure.
4. Expand any row-level EN 301 549 details that still rely on clause-level summary wording.
5. Confirm support workflow accessibility and alternate-format handling for the buyer-facing issue.

## Closed Decisions

- No separate spoken screen-reader matrix is planned for the current buyer issue unless scope changes.
- Sensory-preference themes remain visible in federal-buyer deployments as supplemental user-choice themes outside the conformance set.
- VPAT updates are staged in `current/` and issued only for the frozen production-bound snapshot.

