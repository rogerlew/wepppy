# Geneva Test Data

This directory contains Geneva test fixtures and metadata used by:

- `tests/nodb/mods/geneva/`

Current fixture set:

- `fixtures_manifest.json` defines the fixture catalog contract for WP-00.
- `synthetic_small_watershed_v1/` is a ready synthetic fixture with aligned 4x4 GeoTIFF inputs:
  - `bound.tif`
  - `landuse.tif`
  - `hydgrpdcd.tif`
  - `burn_severity.tif`
  - `fixture_metadata.json`
