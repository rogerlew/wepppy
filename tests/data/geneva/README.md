# Geneva Test Data

This directory contains Geneva test fixtures and metadata used by:

- `tests/nodb/mods/geneva/`

Current state:

- `fixtures_manifest.json` provides the fixture catalog contract for WP-00.
- Fixture entries may be marked `placeholder` until synthetic raster fixtures are added.
